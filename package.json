{
  "name": "ciphersqlstudio",
  "version": "1.0.0",
  "description": "CipherSQLStudio - Master SQL through Interactive Learning",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && npm run build",
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
